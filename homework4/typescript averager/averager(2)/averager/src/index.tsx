import * as React from "react";
import * as ReactDOM from "react-dom";

import { Averager } from "./components/Averager";

ReactDOM.render(
    <Averager />, document.getElementById("display"));